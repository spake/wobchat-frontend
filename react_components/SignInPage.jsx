var React = require('react'),
    SignIn = require('./SignIn.jsx');

module.exports = React.createClass({
    render: function() {
        return (
            <div>
                <SignIn />
            </div>
        );
    }
})
